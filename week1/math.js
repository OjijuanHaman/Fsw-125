const sum = (a,b) => {
    return a + b
};

const substract = (c,d) => {
    return c - d
};

const multiply = (e,f) => {
    return e * f
};

const divide = (g,h) => {
    return g / h
};

module.exports = {
    sum,
    substract,
    multiply,
    divide
}